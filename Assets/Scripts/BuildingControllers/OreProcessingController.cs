using UnityEngine;

public class OreProcessingController : MonoBehaviour
{
    public BuildingState buildingState;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
